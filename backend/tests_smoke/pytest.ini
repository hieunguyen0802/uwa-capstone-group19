[pytest]
# Standalone smoke: no Django integration, hits a live HTTP stack.
# Invoke from repo root:
#   cd backend && pytest -c tests_smoke/pytest.ini tests_smoke/ -v
python_files = test_*.py
addopts = -ra
